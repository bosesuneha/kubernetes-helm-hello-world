apiVersion: v1
kind: Service
metadata:
  name: test-workflow
  namespace: namespace-workflow-1705476526855
spec:
  type: LoadBalancer
  selector:
    app: test-workflow
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000